from setuptools import setup, find_packages

setup(
    name="ielts_speaking_app",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'streamlit',
        # Add other dependencies
    ]
)